'use client'


const SearchBar = () => {
    const [sarch, setSearch] = React.useState(second)
  return (
    <div>
        <input type="text" name="search" id="search" />
    </div>
  )
}

export default SearchBar